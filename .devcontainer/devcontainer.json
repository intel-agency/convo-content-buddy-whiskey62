{
  "name": "convo-content-buddy-whiskey62-devcontainer",
  "image": "ghcr.io/intel-agency/convo-content-buddy-whiskey62/devcontainer:main-latest",
  "containerEnv": {
    "DOTNET_ROOT": "/usr/share/dotnet"
  },
  "remoteEnv": {
    "GITHUB_TOKEN": "${localEnv:GITHUB_TOKEN}",
    "GITHUB_PERSONAL_ACCESS_TOKEN": "${localEnv:GITHUB_TOKEN}",
    "ZHIPU_API_KEY": "${localEnv:ZHIPU_API_KEY}",
    "KIMI_CODE_ORCHESTRATOR_AGENT_API_KEY": "${localEnv:KIMI_CODE_ORCHESTRATOR_AGENT_API_KEY}"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-dotnettools.csdevkit",
        "oven.bun-vscode",
        "avaloniateam.vscode-avalonia"
      ]
    }
  },
  "remoteUser": "vscode"
}
