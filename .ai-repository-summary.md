# ConvoContentBuddy Repository Summary

**AI-Powered Real-time Programming Interview Assistant**

## Repository Overview

This repository contains ConvoContentBuddy, an autonomous background listener designed to assist users during technical interviews and coding sessions. The system uses AI-powered speech recognition and semantic search to automatically identify algorithmic problems and provide optimal solutions.

## Architecture Overview

### Core Components

| Component | Type | Description |
|-----------|------|-------------|
| **ConvoContentBuddy.AppHost** | Aspire Orchestrator | Manages service lifecycle, health checks, and dependency injection |
| **ConvoContentBuddy.API.Brain** | ASP.NET Core Web API | Semantic search endpoints, Gemini embedding generator, pgvector hybrid retrieval |
| **ConvoContentBuddy.Data** | EF Core Class Library | AppDbContext, domain entities (Problem, Tag, ProblemTag, IngestionSnapshot), EF migrations, repositories |
| **ConvoContentBuddy.UI.Web** | Blazor WebAssembly | Zero-interaction dashboard with live transcript and solution cards |
| **ConvoContentBuddy.Data.Seeder** | Worker Service | LeetCode GraphQL ingestion, Gemini batch embedding, vector upsertion to pgvector |
| **ConvoContentBuddy.ServiceDefaults** | Class Library | Shared OTLP configuration, health checks, and resilience policies |

### Infrastructure Dependencies

| Service | Purpose | Technology |
|---------|---------|------------|
| **Qdrant** | Vector similarity search | gRPC on port 6334 |
| **PostgreSQL** | Graph relationships and metadata | pgvector extension |
| **Redis** | SignalR backplane and caching | StackExchange.Redis |
| **Gemini API** | LLM inference and embeddings | Gemini 2.5 Flash, text-embedding-004 |

## Key Design Decisions

### 1. Triple Modular Redundancy (TMR)
- API.Brain runs with 3 replicas
- Redis backplane ensures SignalR connection persistence
- Automatic failover on instance failure

### 2. Hybrid Retrieval Strategy
- **Tier 1**: Qdrant vector search + PostgreSQL graph expansion + Gemini verification
- **Tier 2**: Fallback to secondary LLM provider
- **Tier 3**: Deterministic "Safe Mode" using local vectors only

### 3. Zero-Interaction UI
- Web Speech API for continuous transcription
- SignalR WebSocket for real-time updates
- Autonomous client-side buffer management
- Debounced POST requests to API

## Project Structure

```
/
├── .github/                    # GitHub Actions workflows
│   └── workflows/
├── .opencode/                  # Agent definitions and commands
│   ├── agents/                    # Specialist agent markdown files
│   └── commands/                  # Reusable command prompts
├── docs/                       # Documentation
│   ├── architecture/              # ADRs and architecture docs
│   └── validation/                # Validation reports
├── docker/                    # Docker configurations
│   ├── api-brain.Dockerfile
│   ├── data-seeder.Dockerfile
│   ├── ui-web.Dockerfile
│   ├── nginx.conf
│   └── init-scripts/
├── src/
│   ├── ConvoContentBuddy.AppHost/
│   ├── ConvoContentBuddy.ServiceDefaults/
│   ├── ConvoContentBuddy.API.Brain/
│   │   ├── Controllers/
│   │   ├── Endpoints/          # Minimal API endpoints (ProblemEndpoints)
│   │   ├── Services/           # GeminiEmbeddingGenerator
│   │   ├── Repositories/       # ProblemRepository
│   │   ├── Models/
│   │   └── Hubs/
│   ├── ConvoContentBuddy.Data/
│   │   ├── Entities/           # Problem, Tag, ProblemTag, IngestionSnapshot
│   │   ├── Migrations/
│   │   └── Repositories/
│   ├── ConvoContentBuddy.UI.Web/
│   │   ├── Pages/
│   │   ├── Services/
│   │   ├── Interop/
│   │   └── wwwroot/
│   └── ConvoContentBuddy.Data.Seeder/
│       ├── Services/           # GeminiEmbeddingService, GeminiBatchEmbeddingService, LeetCodeGraphQlClient, SnapshotService, ResilientLeetCodeDataSource
│       └── Models/
├── tests/
│   └── ConvoContentBuddy.Tests/
│       ├── Api/
│       ├── DataLayer/
│       └── Ingestion/
├── scripts/                    # Utility scripts
├── plan_docs/                 # Planning documents
├── global.json                # .NET SDK version (10.0.100)
├── docker-compose.yml         # Local development
├── opencode.json              # opencode agent config (MCP servers, ZAI model provider)
├── ConvoContentBuddy.slnx     # Solution file
└── README.md                  # Main documentation
```

## Development Workflow

### Branch Strategy
- `main` - Production-ready code
- `dynamic-workflow-project-setup` - Current development branch
- Feature branches follow pattern: `feature/<feature-name>`

### Build & Test
```bash
# Build solution
dotnet build ConvoContentBuddy.slnx

# Run tests
dotnet test

# Run with coverage
dotnet test --collect:"XPlat Code Coverage"
```

### Local Development
```bash
# Start infrastructure
docker-compose up -d postgres qdrant redis

# Run API (with hot reload)
dotnet watch --project src/ConvoContentBuddy.API.Brain

# Run Blazor UI
dotnet watch --project src/ConvoContentBuddy.UI.Web

# Run Aspire orchestrator
dotnet run --project src/ConvoContentBuddy.AppHost
```

## Configuration

### Required Environment Variables

| Variable | Description | Required By |
|----------|-------------|-------------|
| `GEMINI_API_KEY` | Google Gemini API key | API.Brain, Data.Seeder |

### Configuration Files

| File | Purpose |
|------|---------|
| `appsettings.json` | Base configuration |
| `appsettings.Development.json` | Development overrides |
| `docker-compose.yml` | Docker Compose configuration |
| `docker-compose.override.yml` | Local development overrides |

## Testing Strategy

### Unit Tests
- Located in `tests/ConvoContentBuddy.Tests/`
- Target coverage: 80%+
- Use xUnit, Moq, FluentAssertions

### Integration Tests
- Test service interactions
- Verify SignalR connectivity
- Validate vector search accuracy

### E2E Tests
- Latency validation (< 2s end-to-end)
- TMR verification
- Failover scenarios

## Performance Targets

| Metric | Target | Current Status |
|--------|--------|----------------|
| Semantic vector matching | < 500ms | ✅ Implemented (pgvector + Gemini embeddings) |
| End-to-end processing | < 2s | 🚧 Not yet validated |
| Problem identification accuracy | ≥ 95% | 🚧 Not yet validated |
| Single-container failure recovery | 100% | 🚧 Not yet validated |

## Implementation Phases

### ✅ Phase 1: High-Availability Foundation (Complete)
- Solution structure created
- Aspire orchestration configured (Postgres+pgvector, Redis, all services)
- OpenTelemetry integration ready

### ✅ Phase 2: Semantic Knowledge Ingestion (Complete)
- LeetCode GraphQL client implemented (`LeetCodeGraphQlClient`, `ResilientLeetCodeDataSource`)
- Gemini embedding services implemented (`GeminiEmbeddingService`, `GeminiBatchEmbeddingService`)
- Snapshot capture service implemented (`SnapshotService`)
- `ProblemIngestionWorker` wires the full ingestion pipeline
- EF Core data layer with pgvector (`ConvoContentBuddy.Data`: entities, migrations, repositories)

### ✅ Phase 3: Hybrid Intelligence "Brain" (Complete)
- Semantic vector search via `GeminiEmbeddingGenerator` + pgvector
- `ProblemRepository` with vector similarity queries
- `ProblemEndpoints` minimal API with search functionality
- OpenAPI configuration with full documentation

### 📋 Phase 4: Aerospace-Grade Redundancy
- Failover manager pending
- Multi-tier fallbacks pending
- Deterministic safe mode pending

### 📋 Phase 5: Ambient Real-time Interface
- UI scaffolded (Blazor WASM)
- Speech interop and SignalR hub pending
- Autonomous UI components pending

## Observability

### OpenTelemetry Integration
- **Traces**: Distributed request tracing
- **Metrics**: Performance counters
- **Logs**: Structured logging via ASP.NET Core logging + OpenTelemetry

### Health Checks
- `/health` - Overall health status
- `/health/ready` - Readiness probe
- `/health/live` - Liveness probe

## Security Considerations

- API keys stored in environment variables
- HTTPS enforced in production
- SignalR connections authenticated
- CORS configured for Blazor client
- Input validation on all endpoints

## Deployment

### Azure (azd)
- Infrastructure as Code with Bicep
- Container Apps for services
- Managed PostgreSQL and Redis
- Azure AI for Gemini integration

### Docker Compose
- Local development and testing
- 3 API replicas for TMR testing
- Volume persistence for databases

## Known Issues & Limitations

1. **SemanticKernel Vulnerability**: Current version has known vulnerability (GHSA-2ww3-72rp-wpp4)
   - Suppressed during scaffolding with NoWarn
   - Will be addressed in implementation phase

2. **Web Speech API**: Browser compatibility required
   - Chrome, Edge, Safari supported
   - Firefox requires flags

3. **Latency**: End-to-end latency depends on Gemini API response times

## Contributing

1. Fork the repository
2. Create a feature branch
3. Make changes with tests
4. Ensure all tests pass: `dotnet test`
5. Submit pull request

## License

MIT License - See [LICENSE](LICENSE) for details

## Support

- **Documentation**: [docs/](docs/)
- **Issues**: GitHub Issues
- **Architecture**: [docs/architecture/](docs/architecture/)

---

**Last Updated**: 2026-03-16  
**Repository**: intel-agency/convo-content-buddy-whiskey62  
**Active Branch**: mn/tracyer-t2
